[package]
name = "physics"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2.79"
glam = "0.20.2"

[package.metadata.wasm-pack.profile.release]
wasm-opt = ["-O", "--detect-features"]
